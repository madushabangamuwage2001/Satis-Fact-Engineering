{
  "name": "satis-fact-engineering",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd nern-website/frontend && npm install && npm run build && cp -r dist/* ../../",
    "dev": "cd nern-website/frontend && npm run dev",
    "start": "cd nern-website/frontend && npm run preview"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
